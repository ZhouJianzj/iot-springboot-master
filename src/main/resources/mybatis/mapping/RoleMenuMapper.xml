<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.myiot.myserver.mapper.system.RoleMenuMapper">
  <resultMap id="BaseResultMap" type="com.myiot.myserver.data.po.system.RoleMenu">
    <!--
      WARNING - @mbg.generated
      This element is automatically generated by MyBatis Generator, do not modify.
    -->
    <result column="role_id" jdbcType="VARCHAR" property="roleId" />
    <result column="menu_id" jdbcType="BIGINT" property="menuId" />
  </resultMap>
  <insert id="insert" parameterType="com.myiot.myserver.data.po.system.RoleMenu">
    <!--
      WARNING - @mbg.generated
      This element is automatically generated by MyBatis Generator, do not modify.
    -->
    insert into myiot_role_menu (role_id, menu_id)
    values (#{roleId,jdbcType=VARCHAR}, #{menuId,jdbcType=BIGINT})
  </insert>
  <select id="selectAll" resultMap="BaseResultMap">
    <!--
      WARNING - @mbg.generated
      This element is automatically generated by MyBatis Generator, do not modify.
    -->
    select role_id, menu_id
    from myiot_role_menu
  </select>

  <insert id="insertBatch" parameterType="java.util.List">
    INSERT INTO myiot_role_menu (role_id, menu_id)
    VALUES
        <foreach collection="roleMenuList" item="roleMenu" separator=",">
            (#{roleMenu.roleId}, #{roleMenu.menuId})
        </foreach>
  </insert>

  <select id="queryRoleMenus" parameterType="java.lang.String"
          resultType="com.myiot.myserver.data.po.system.Menu">
    SELECT
        m.id AS id,
        m.name AS name,
        m.code AS code,
        m.parent_id AS parentId,
        m.seq AS seq,
        m.icon AS icon,
        m.url AS url,
        m.leaf AS leaf
    FROM myiot_role_menu rm
    LEFT JOIN myiot_menu m ON rm.menu_id = m.id
    WHERE rm.role_id = #{roleId}
  </select>

    <delete id="removeByRoleId" parameterType="java.lang.String">
        DELETE FROM myiot_role_menu
        WHERE role_id = #{roleId}
    </delete>

    <delete id="deleteRoleMenus" parameterType="java.util.List">
        DELETE FROM myiot_role_menu
        WHERE role_id IN
        <foreach collection="roleIds" item="roleId" open="(" separator="," close=")">
            #{roleId}
        </foreach>
    </delete>
</mapper>
