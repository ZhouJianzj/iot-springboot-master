<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.myiot.myserver.mapper.system.MenuMapper">
  <resultMap id="BaseResultMap" type="com.myiot.myserver.data.po.system.Menu">
    <!--
      WARNING - @mbg.generated
      This element is automatically generated by MyBatis Generator, do not modify.
    -->
    <id column="id" jdbcType="BIGINT" property="id" />
    <result column="name" jdbcType="VARCHAR" property="name" />
    <result column="code" jdbcType="VARCHAR" property="code" />
    <result column="parent_id" jdbcType="BIGINT" property="parentId" />
    <result column="seq" jdbcType="INTEGER" property="seq" />
    <result column="icon" jdbcType="VARCHAR" property="icon" />
    <result column="url" jdbcType="VARCHAR" property="url" />
    <result column="leaf" jdbcType="TINYINT" property="leaf" />
  </resultMap>
  <delete id="deleteByPrimaryKey" parameterType="java.lang.Long">
    <!--
      WARNING - @mbg.generated
      This element is automatically generated by MyBatis Generator, do not modify.
    -->
    delete from myiot_menu
    where id = #{id,jdbcType=BIGINT}
  </delete>
  <insert id="insert" parameterType="com.myiot.myserver.data.po.system.Menu">
    <!--
      WARNING - @mbg.generated
      This element is automatically generated by MyBatis Generator, do not modify.
    -->
    insert into myiot_menu (id, `name`, code,
      parent_id, seq, icon,
      url, leaf)
    values (#{id,jdbcType=BIGINT}, #{name,jdbcType=VARCHAR}, #{code,jdbcType=VARCHAR},
      #{parentId,jdbcType=BIGINT}, #{seq,jdbcType=INTEGER}, #{icon,jdbcType=VARCHAR},
      #{url,jdbcType=VARCHAR}, #{leaf,jdbcType=TINYINT})
  </insert>
  <update id="updateByPrimaryKey" parameterType="com.myiot.myserver.data.po.system.Menu">
    <!--
      WARNING - @mbg.generated
      This element is automatically generated by MyBatis Generator, do not modify.
    -->
    update myiot_menu
    set `name` = #{name,jdbcType=VARCHAR},
      code = #{code,jdbcType=VARCHAR},
      parent_id = #{parentId,jdbcType=BIGINT},
      seq = #{seq,jdbcType=INTEGER},
      icon = #{icon,jdbcType=VARCHAR},
      url = #{url,jdbcType=VARCHAR},
      leaf = #{leaf,jdbcType=TINYINT}
    where id = #{id,jdbcType=BIGINT}
  </update>
  <select id="selectByPrimaryKey" parameterType="java.lang.Long" resultMap="BaseResultMap">
    <!--
      WARNING - @mbg.generated
      This element is automatically generated by MyBatis Generator, do not modify.
    -->
    select id, `name`, code, parent_id, seq, icon, url, leaf
    from myiot_menu
    where id = #{id,jdbcType=BIGINT}
  </select>
  <select id="selectAll" resultMap="BaseResultMap">
    <!--
      WARNING - @mbg.generated
      This element is automatically generated by MyBatis Generator, do not modify.
    -->
    select id, `name`, code, parent_id, seq, icon, url, leaf
    from myiot_menu
  </select>

  <select id="countByName" resultType="java.lang.Integer">
    SELECT COUNT(id)
    FROM myiot_menu
    WHERE parent_id = #{parentId} AND name = #{name}
  </select>

  <delete id="deleteMenus" parameterType="java.util.List">
    DELETE FROM myiot_menu
    WHERE id IN
    <foreach collection="menuIds" item="menuId" open="(" separator="," close=")">
      #{menuId}
    </foreach>
  </delete>

  <select id="queryAll" resultType="com.myiot.myserver.data.vo.system.MenuInfo">
    SELECT
        CAST(id AS CHAR) id,
        name AS name,
        code AS code,
        CAST(parent_id AS CHAR) parentId,
        seq AS seq,
        icon AS icon,
        url AS url,
        leaf AS leaf
    FROM myiot_menu
  </select>

  <select id="queryById" parameterType="java.lang.Long"
          resultType="com.myiot.myserver.data.vo.system.MenuInfo">
    SELECT
        CAST(menu.id AS CHAR) id,
        menu.name AS name,
        menu.code AS code,
        CAST(menu.parent_id AS CHAR) parentId,
        menu.seq AS seq,
        menu.icon AS icon,
        menu.url AS url,
        menu.leaf AS leaf
    FROM myiot_menu menu
    WHERE menu.id = #{menuId}
  </select>
</mapper>
